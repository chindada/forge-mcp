# forge-mcp

Stdio MCP server exposing one tool, `run_forge`, that orchestrates a
Planner → Generator → Evaluator loop to autonomously implement a feature
described in a design document. Clean-room re-architecture of
`harness-mcp` minus Playwright.

See `docs/specs/forge-mcp-design.md` for the implementation brief.
